{
  "name": "kipi-brand-consulting",
  "private": true,
  "version": "1.0.0",
  "description": "KIPRIS 특허/상표/실용신안 검색 및 컴플라이언스 문의 시스템 (모노레포 루트)",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "build": "npm install --prefix server && npm install --prefix client && npm run build --prefix client",
    "start": "npm start --prefix server"
  }
}
